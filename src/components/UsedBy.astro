
---
const brands = ['Google','Microsoft','Porsche','OpenAI','Netlify','Cloudflare','NBC','Guardian','Michelin','Proton'];
---
<section class="mx-auto max-w-6xl px-6 py-10">
  <p class="text-center text-sm text-slate-500">Used by teams around the world</p>
  <div class="mt-6 grid grid-cols-2 sm:grid-cols-5 gap-6 opacity-70">
    {brands.map((n) => (
      <div class="h-10 rounded bg-slate-100 flex items-center justify-center text-slate-400">{n}</div>
    ))}
  </div>
</section>
